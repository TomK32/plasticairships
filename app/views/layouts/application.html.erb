<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.1//EN"
"http://www.w3.org/TR/xhtml11/DTD/xhtml11.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en">

<head>
  <title><%= @page_title.blank? ? Goldberg.settings.site_name : ('%s // %s' % [@page_title, Goldberg.settings.site_name]) %></title>

  <meta http-equiv="content-type" content="text/html; charset=UTF-8" />
  <meta name="description" content="plasticpilots is showcasing great webdesign" />
  <meta name="author" content="plasticpilots.com" />
  <meta name="keywords" content="webdesign,showcases,design" />
	
  <meta name="robots" content="all" />
  <link title="Sites" type="application/atom+xml" href="<%= formatted_sites_url(:format => :rss)%>" rel="alternate" />
  <link title="Blog" type="application/atom+xml" href="<%= formatted_posts_url(:format => :rss)%>" rel="alternate" />

  <link rel="stylesheet" href="/goldberg/stylesheets/goldberg.css" type="text/css" media="screen" />
  <link rel="stylesheet" href="/stylesheets/application.css" type="text/css" media="screen" />



</head>

<body>
<div id="container">

<div id="header">

  <h1><a href="/" title="back to home-page">plasticpilots</a></h1>

  <div id="top_navigation">
    <ul>
      <li><%= link_to_unless_current 'sites', sites_url %></li>
      <li><%= link_to_unless_current 'blog', posts_url %></li>
      <li>about</li>
<!--      <li>casteljau</li>
      <li>community</li>-->
      <% if current_user %>
      <li><%= link_to 'profile', profile_url %></li>
      <% if current_user.moderator? %>
      <li>dashboard</li>
      <% end %>
      <% if current_user.admin? %>
      <li><%= link_to 'admin', '/site_admin' %></li>
      <% end %>
      <li><%= link_to 'logout', logout_url %></li>
      <% else %>
      <li><%= link_to 'login', login_url %></li>
      <li><%= link_to 'signup', signup_url %></li>
      <% end %>
    </ul>
    <%= render :partial => 'navigation' if params[:controller] == 'sites' %>
  </div>

</div>


<div id="left">
  <h3>Blog<%= link_to '++', new_post_url(), :title => 'submit a new blog post', :class => "plus" %></h3>
  <% cache('posts-sidebar') do %>
    <% if latest_posts.blank? %>
    <p>No posts so far</p>
    <% else %>
      <% latest_posts.each do |post| %>
      <h4><%= link_to post.title, post_url(post) %></h4>
      <%= textilize(post.excerpt) %>
      <% end %>
      <%= link_to 'more blog posts', posts_url() %>
    <% end %>
  <% end %>
    <%= goldberg_left_menu %>
</div>


<a name="top"></a>
<div id="main">
<% if @page_title %>
  <h2><%= @page_title %></h2>
<% end %>
<% flash.each do |type, message| %>
  <div class="<%= type %>">
    <%= message %>
  </div>
<% end %>

<%= yield %>
</div>

<div id="right">
  <h3>Sites<%= link_to '++', new_site_url(), :title => 'submit a new site', :class => "plus" %></h3>
  <% cache('sites-sidebar') do %>
    <% if latest_sites(25).blank? %>
    <p>No sites so far</p>
    <% else %>
    <ul id="sites-list">
      <% latest_sites(25).each do |site| %>
      <li><%= link_to site.title, site %></li>
      <% end %>
    </ul>
    <%= link_to 'more sites', sites_url() %>        
    <% end %>      
  <% end %>
</div>
<div id="footer">
<ul>
  <li><a href="#top">^ top</a></li>
  <li><a href="/">home</a></li>
  <li><%= link_to 'RSS blog', formatted_posts_url(:format => :atom) %></li>
  <li><%= link_to 'RSS sites', formatted_sites_url(:format => :atom) %></li>
  <li>powered with <a href="http://github.com/TomK32/plasticairships/" title="plasticairships is the software behind plasticpilots and best of all, it's open source!">plasticairships</a></li>
</ul>  
<%= Goldberg.settings.footer_message %>
</div>
</body>
</html>