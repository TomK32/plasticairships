<% @page_title = 'Add a new site' %>

<%= error_messages_for :site %>

<% form_for(:site, @site, :url => sites_url) do |f| %>
  <%= render :partial => 'form' , :locals => {:f => f} %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', sites_path %>
